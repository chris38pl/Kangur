/**
 * Web: Expo push / ExpoDevice are not available. Keep this module free of
 * expo-notifications and expo-device so ExpoRoot can boot in the browser.
 */
export function usePushRegistration(_enabled: boolean): void {
  // no-op
}
