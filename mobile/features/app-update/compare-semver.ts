/** @deprecated Import from `@/lib/semver` — kept for existing app-update imports. */
export {
  parsePlainSemver,
  compareSemver,
  isVersionNewer,
  isNewerVersion,
  type SemverParts,
} from "@/lib/semver";
